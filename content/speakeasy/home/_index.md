---
title: Home
build:
  render: never
  list: never
cascade:
  build:
    render: never
    list: local
---

Homepage sections. Each child is a band on `/`. Card grids (features, clients, steps, screenshots, promise) come from Markdown files in that section folder. These pages are not published at their own URLs.
