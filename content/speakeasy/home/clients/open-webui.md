---
title: Open WebUI
weight: 30
---

1. Admin Settings → Connections (or user Settings → Connections).
2. Enable the OpenAI API connection.
3. Set API Base URL to `http://<proxy>/v1` — it must end in `/v1`.
4. Set API Key to any placeholder, then save. Mesh models show up in the picker.
